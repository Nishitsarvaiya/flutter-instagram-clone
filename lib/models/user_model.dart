class User {
  String fullname;
  String username;
  String profilePicture;
  int posts;
  int followers;
  int followings;

  User({this.fullname, this.username, this.profilePicture, this.posts, this.followers, this.followings});
}
